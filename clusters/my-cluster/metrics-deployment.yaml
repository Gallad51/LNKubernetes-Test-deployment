apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: metrics
  template:
    metadata:
      labels:
        app: metrics
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: metrics
          image: ghcr.io/gallad51/lnkubernetes-test/metrics:1.0.21 # {"$imagepolicy": "flux-system:metrics"}
          ports:
            - containerPort: 8080
